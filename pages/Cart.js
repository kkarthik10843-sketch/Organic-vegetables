function Cart() {
    const items = window.AgriDirect.cart;
    const subtotal = items.reduce((total, item) => total + item.price * item.quantity, 0);
    const delivery = items.length ? 30 : 0;
    return <div className="page-shell" data-name="cart-page"><div className="mb-8"><p className="section-kicker">Your harvest basket</p><h1 className="text-4xl mb-2">Shopping cart</h1><p className="text-slate-500">Fresh produce, packed at the source and delivered to your door.</p></div>{items.length === 0 ? <div className="card p-12 text-center"><div className="icon-shopping-basket text-5xl text-green-200 mb-5"></div><h2 className="text-2xl mb-2">Your cart is waiting</h2><p className="text-slate-500 mb-6">Add something fresh from the marketplace.</p><a href="#products" className="btn-primary">Explore marketplace</a></div> : <div className="grid lg:grid-cols-[1fr_360px] gap-8"><div className="space-y-4">{items.map(item => <div className="card p-4 flex flex-col sm:flex-row sm:items-center gap-4" key={item.id}><ProductImage src={item.image} alt={item.name} className="w-full sm:w-24 h-24 rounded-lg shrink-0" /><div className="flex-grow"><h3 className="font-bold text-lg">{item.name}</h3><p className="text-sm text-slate-500">{item.farmer} · ₹{item.price}/{item.unit}</p></div><div className="flex items-center gap-3"><button aria-label="Decrease quantity" onClick={() => window.AgriDirect.updateCart(item.id, -1)} className="w-8 h-8 rounded-full border"><div className="icon-minus text-sm"></div></button><b>{item.quantity}</b><button aria-label="Increase quantity" onClick={() => window.AgriDirect.updateCart(item.id, 1)} className="w-8 h-8 rounded-full border"><div className="icon-plus text-sm"></div></button></div><b className="text-lg sm:w-24 sm:text-right">₹{item.price * item.quantity}</b><button aria-label="Remove item" onClick={() => window.AgriDirect.removeFromCart(item.id)} className="text-red-500 p-2"><div className="icon-trash-2"></div></button></div>)}</div><div className="card p-6 h-fit"><h2 className="text-xl mb-5">Order summary</h2><div className="space-y-3 text-slate-500 mb-6"><div className="flex justify-between"><span>Subtotal</span><span>₹{subtotal}</span></div><div className="flex justify-between"><span>Delivery</span><span>₹{delivery}</span></div><div className="border-t pt-4 flex justify-between text-lg font-bold text-slate-900"><span>Total</span><span className="text-[var(--primary)]">₹{subtotal + delivery}</span></div></div><button onClick={() => window.location.hash = '#checkout'} className="btn-primary w-full">Checkout <div className="icon-arrow-right"></div></button></div></div>}</div>;
}